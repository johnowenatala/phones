var React = require('react');

var Spam = React.createClass({

  render: function() {
    return (
        <span className="this-is-spam">
          SPAM
        </span>
    );
  }
});

module.exports = Spam;

